section class=articles-carousel
  h2Articolih2
  div class=carousel-container
    button class=prev&#10094;button
    div class=carousel
      article class=card
        h3Articolo 1h3
        pDescrizione breve dell’articolo 1.p
        a href=#Leggi di piùa
      article
      article class=card
        h3Articolo 2h3
        pDescrizione breve dell’articolo 2.p
        a href=#Leggi di piùa
      article
      article class=card
        h3Articolo 3h3
        pDescrizione breve dell’articolo 3.p
        a href=#Leggi di piùa
      article
    div
    button class=next&#10095;button
  div
section

style
.articles-carousel {
  text-align center;
  margin 3rem auto;
  max-width 800px;
  font-family Roboto, sans-serif;
}

.carousel-container {
  position relative;
  overflow hidden;
}

.carousel {
  display flex;
  transition transform 0.5s ease-in-out;
}

.card {
  min-width 100%;
  box-sizing border-box;
  padding 2rem;
  background #f9f9f9;
  margin 0 5px;
  border-radius 12px;
  box-shadow 0 4px 10px rgba(0,0,0,0.1);
}

button.prev,
button.next {
  position absolute;
  top 10px;
  background none;
  border none;
  font-size 2rem;
  cursor pointer;
  color #333;
  padding 0 10px;
  z-index 10;
}

button.prev { left 0; }
button.next { right 0; }

buttonhover {
  color #0073e6;
}
style

script
const carousel = document.querySelector(.carousel);
const cards = document.querySelectorAll(.card);
const prevBtn = document.querySelector(.prev);
const nextBtn = document.querySelector(.next);

let index = 0;

function showSlide(i) {
  if (i  0) index = cards.length - 1;
  else if (i = cards.length) index = 0;
  else index = i;

  carousel.style.transform = `translateX(${-index  100}%)`;
}

prevBtn.addEventListener(click, () = showSlide(index - 1));
nextBtn.addEventListener(click, () = showSlide(index + 1));
script